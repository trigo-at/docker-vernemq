PROJECT: vernemq
BUILD_TYPE: monorepo
PUBLISH_CONFIG:
  master:
    cluster: okd01
    environments:
      - staging
    services:             # only build these services on main
      - vernemq-trigo
TAG_ONLY: true
SKIP_TESTS: true
DOCKER_BUILDKIT: '1'
BUILD_CONTEXT: .
# SOURCE_ROOT: ./vernemq-trigo
